#============= ueventd ==============
# FIXME: are all of these rules needed?
allow ueventd tmpfs:chr_file relabelfrom;
allow ueventd tmpfs:dir { add_name relabelfrom write };
allow ueventd tmpfs:lnk_file create;

allow ueventd mtd_device:chr_file r_file_perms;

allow ueventd configfs:{ file lnk_file } create_file_perms;
allow ueventd configfs:dir create_dir_perms;

allow ueventd device:chr_file relabelto;
allow ueventd device:dir relabelto;
